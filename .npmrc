# Cloudflare Pages runs `npm clean-install` (npm ci) which is strict about
# peer-dependency conflicts. `@cloudflare/next-on-pages` has one minor conflict
# with our Next.js version that's safe to ignore, so we accept legacy resolution.
legacy-peer-deps=true
